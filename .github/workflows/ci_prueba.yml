# .github/workflows/ci_prueba.yml
name: CI - Prueba y Compilacion

on:
  pull_request:
    branches: [ "main" ]

jobs:
  construir_test:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v código final y limpio:**

```yaml
# .github/workflows/ci_prueba.yml
name: CI - Prueba y Compilacion

on:
  pull_request:
    branches: [ "main", "develop" ] # Lo haremos para PRs a main y develop

jobs:
  construir_y_probar:
    name: Compilar y Probar en PR
    runs-on: ubuntu-latest
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Simular compilación del proyecto
        run: |
          echo "🔧 Compilando proyecto..."
          if [ -f carta_digital/carta.md ]; then
            echo "✅ Archivo carta_digital/carta.md encontrado"
          else
            echo "❌ Archivo carta_digital/carta.md no encontrado"
            exit 1
          fi

      - name: Simular verificación del contenido
        run: |
          echo "🔍 Verificando contenido del archivo..."
          grep -q "Carta Digital" carta_digital/carta.md && echo "✅ Contenido válido" || (echo "❌ Título incorrecto" && exit 1)